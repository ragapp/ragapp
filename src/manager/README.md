# RAGapp agent manager
This is a manager app designed to help you manage the RAGapp agent running in Docker. 

# Development:
- Install dependencies
```shell
poetry install
pnpm --prefix ./frontend install
```
- Run app:
```shell
make dev
```
